name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Test, lint, and package
    runs-on: macos-26
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Tool versions
        run: |
          xcodebuild -version
          swift --version

      - name: Install lint tools
        run: brew install swiftlint swiftformat

      - name: Run checks
        run: REQUIRE_SWIFTLINT=1 REQUIRE_SWIFTFORMAT=1 scripts/check.sh
